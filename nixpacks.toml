[start]
cmd = "cd backend && python manage.py collectstatic --noinput && python manage.py migrate --noinput && python manage.py ensure_superuser && gunicorn breathe_esg.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file -"
